{
  "buildCommand": "cd vite-latest && npm install && npm run build",
  "outputDirectory": "vite-latest/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_OPTIONS": "--max-old-space-size=3072"
  }
}
